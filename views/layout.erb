<!DOCTYPE html>
<html>
	<head>
		<title>Alexandra Tsareva : Cakes & Sweets (Sinatra version)</title>
			
			<!--Import Google Icon Font-->
      <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

      <!--Import Font Awesome-->
      <script defer src="https://use.fontawesome.com/releases/v5.0.7/js/all.js"></script>
            
      <!--Import Fonts-->
      <link href="https://fonts.googleapis.com/css?family=Amatic+SC|Cuprum" rel="stylesheet">

      <!--Import materialize.css-->
      <link type="text/css" rel="stylesheet" href="/css/materialize.min.css"  media="screen,projection"/>
      
      <!--Import css file-->
      <link type="text/css" rel="stylesheet" href="/css/main.css"/>
      <link type="text/css" rel="stylesheet" href="/css/shop.css"/>

      <!--Let browser know website is optimized for mobile-->
      <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
		
	</head>
	
	<body>

    <nav>
      <div class="nav-wrapper">
        <a href="#" data-activates="mobile-demo" class="button-collapse brand-logo center"><i class="material-icons">menu</i></a>
        <ul class="brand-logo center hide-on-med-and-down">
          <li><a href="<%= @language == 'rus' ? '/blog' : '/eng/blog' %>" class="first-nav-link">&#8195;&#8195;<%= @language == 'rus' ? 'Блог' : 'Blog' %>&#8195;&#8195;</a></li>
          <li><a href="<%= @language == 'rus' ? '/gallery' : '/eng/gallery' %>" class="first-nav-link">&#8195;&#8195;<%= @language == 'rus' ? 'Галерея' : 'Gallery' %>&#8195;&#8195;</a></li>
          <li><a href="<%= @language == 'rus' ? '/q&a' : '/eng/q&a' %>" class="first-nav-link">&#8195;&#8195;<%= @language == 'rus' ? 'Вопросы и ответы' : 'Q & A' %>&#8195;&#8195;</a></li>
          <li><a href="<%= @language == 'rus' ? '/contacts' : '/eng/contacts' %>" class="first-nav-link">&#8195;&#8195;<%= @language == 'rus' ? 'Контакты' : 'Contacts' %>&#8195;&#8195;</a></li>
        </ul>
        <ul class="side-nav" id="mobile-demo">
          <p class="side-navigation"></p>
          <li><a href="<%= @language == 'rus' ? '/blog' : '/eng/blog' %>"><%= @language == 'rus' ? 'Блог' : 'Blog' %></a></li>
          <li><a href="<%= @language == 'rus' ? '/gallery' : '/eng/gallery' %>"><%= @language == 'rus' ? 'Галерея' : 'Gallery' %></a></li>
          <li><a href="<%= @language == 'rus' ? '/q&a' : '/eng/q&a' %>"><%= @language == 'rus' ? 'Вопросы и ответы' : 'Q & A' %></a></li>
          <li><a href="<%= @language == 'rus' ? '/contacts' : '/eng/contacts' %>"><%= @language == 'rus' ? 'Контакты' : 'Contacts' %></a></li>
        </ul>
      </div>
    </nav>

    <div>
      <%= yield %>
    </div>

    <div class="free-space-bottom"></div>
    
    <div class="container"><a href="<%= @language == 'rus' ? '/' : '/eng/' %>"><%= @language == 'rus' ? 'Взад' : 'Home' %></a></div>

  	<!--Import jQuery before materialize.js-->
    <script type="text/javascript" src="/vendor/jquery-3.2.0.min.js"></script>
    <script type="text/javascript" src="/js/materialize.min.js"></script>
    
    <!--Import vanilla.js-->
    <script type="text/javascript" src="/js/app.js"></script>

  </body>

</html>
